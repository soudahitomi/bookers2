<div class= "container" >
  <div class= "row">
    <div class="col-md-3"><!--col-md-3の開始タグ-->

<%= render partial: 'users/list', locals: { user: @books.user, book: @book } %>

    </div><!--col-md-3の閉じタグ-->

    <div class="col-md-8 offset-md-1">
      <h1>Book detail</h1>
        <table class="table table-striped">
          <tbody>
            <tr>
              <td><%= link_to user_path(@books.user.id) do %>
                <%= image_tag @books.user.get_profile_image(100,100) %>
                <div><%= @books.user.name %></div>
                <% end %>
              </td>

              <td>
                <%= link_to book_path(@books.user.id) do %>
                <%= @books.title %>
                <% end %>
              </td>

              <td>
                <%= @books.body %>
              </td>

              <td>
                <% if @books.user == current_user %>
                  <%= link_to edit_book_path(@books.user.id), class: "btn btn-success" do %><!--ここの@books.user.idがなかなかわからなかった-->
                    Edit
                  <% end %>
                <% end %>
              </td>

              <td>
                <% if @books.user == current_user %>
                  <%= link_to book_path(@books), method: :delete, class: "btn btn-danger" do %>
                    Destroy
                  <% end %>
                <% end %>
              </td>
            </tr>

          </tbody>
        </table>
    </div><!--col-md-8 offsetの閉じタグ-->
  </div><!--rowの閉じタグ-->
</div><!--contanerの閉じタグ-->